<h3>Quiz Details</h3>

<div class="well">
  <p>
    <strong>Name:</strong>
	  <%= @quiz.name %>
  </p>
  </div>
<p>
  <%= link_to "Edit", edit_quiz_path(@quiz), :class => 'btn' %> |
  <%= link_to "Destroy", @quiz, :confirm => 'Are you sure?', :method => :delete, :class => 'btn' %> |
  <%= link_to "View All", quizzes_path, :class => 'btn btn-danger' %>
</p>